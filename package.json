{
  "name": "afst",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "./node_modules/.bin/jest",
    "postinstall": "npm run build"
  },
  "author": "Sebastiaan de Geus",
  "license": "ISC",
  "dependencies": {
    "@types/dotenv": "^6.1.0",
    "@types/jest": "^24.0.11",
    "dotenv": "^6.2.0",
    "jest": "^24.5.0",
    "require-env": "^0.2.1",
    "ts-jest": "^24.0.0",
    "ts-node": "^7.0.0",
    "typescript": "^3.0.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "prettier --write",
      "git add"
    ]
  },
  "devDependencies": {
    "husky": "^1.3.1",
    "lint-staged": "^8.1.4",
    "prettier": "^1.16.4"
  }
}
